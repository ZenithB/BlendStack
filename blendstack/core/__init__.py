"""BlendStack core engine (project brief §3).

Pure processing engine: NumPy image maths, Pillow/rawpy/imageio file I/O.
Zero UI dependencies and zero GIMP dependencies — both the standalone app
and the GIMP plugin import this package.

The public entry point for frontends is :mod:`blendstack.core.engine`.
"""
